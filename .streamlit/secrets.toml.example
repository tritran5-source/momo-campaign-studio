# MẪU secrets — KHÔNG commit file secrets.toml thật lên GitHub.
#
# Cách dùng:
#  • Streamlit Cloud: KHÔNG cần file này. Vào App > Settings > Secrets, dán đúng 1 dòng dưới.
#  • Chạy local có secrets: đổi tên file này thành "secrets.toml" và điền key.
#
GEMINI_API_KEY = "AQ.dan-key-Gemini-cua-ban-vao-day"
